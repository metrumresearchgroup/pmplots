% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pmp-gg-labs.R
\name{pmp_relabel_pairs}
\alias{pmp_relabel_pairs}
\title{Relabel facets in a pmplots pairs plot}
\usage{
pmp_relabel_pairs(
  p,
  spec,
  labs = list(),
  short_max = Inf,
  unit_break = TRUE,
  ...
)
}
\arguments{
\item{p}{a pairs plot object created by \code{\link[=eta_pairs]{eta_pairs()}} or \code{\link[=pairs_plot]{pairs_plot()}}.}

\item{spec}{a named list of label data; names correspond to columns
in the data used to make the plot; may also be a \code{yspec} object, which
will be converted to a named list through \code{\link[yspec:ys_get_short_unit]{yspec::ys_get_short_unit()}}.}

\item{labs}{another named list of label data to override names found in
\code{spec}.}

\item{short_max}{passed to \code{\link[yspec:ys_get_short_unit]{yspec::ys_get_short_unit()}} when \code{spec} is a
\code{yspec} object.}

\item{unit_break}{if \code{TRUE} (the default), a newline is inserted between the
label text and a trailing parenthetical unit (e.g., \code{"Weight (kg)"} becomes
\code{"Weight\\n(kg)"}).}

\item{...}{currently not used.}
}
\value{
The pairs plot \code{p} with updated facet labels.
}
\description{
This function relabels the axis strip labels in a pairs plot created by
\code{\link[=eta_pairs]{eta_pairs()}} or \code{\link[=pairs_plot]{pairs_plot()}} by looking up the variable names in a named
list or \code{yspec} object. Variable names are discovered automatically from the
plot object. Names absent from \code{spec} and \code{labs} are left unchanged.
}
\examples{
id <- pmplots_data_id()

etas <- c("ETA1//ETA-CL", "ETA2//ETA-VC", "ETA3//ETA-KA")

spec <- list(ETA1 = "ETA on CL (L/h)", ETA2 = "ETA on Vc (L)")

p <- eta_pairs(id, etas)

pmp_relabel_pairs(p, spec)

}
\seealso{
\code{\link[=pmp_relabel]{pmp_relabel()}}, \code{\link[=pmp_relabel_wrap]{pmp_relabel_wrap()}}, \code{\link[=pmp_gg_labs]{pmp_gg_labs()}}
}
