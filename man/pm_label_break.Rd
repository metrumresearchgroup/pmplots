% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pm-gg-labs.R
\name{pm_label_break}
\alias{pm_label_break}
\title{Labeller to break a facet label into two lines}
\usage{
pm_label_break(width)
}
\arguments{
\item{width}{character width at which to insert the line break; the label
is split at the last word boundary at or before this position.}
}
\value{
A labeller function suitable for the \code{labeller} argument of
\code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}} or \code{\link[ggplot2:facet_grid]{ggplot2::facet_grid()}}.
}
\description{
Returns a ggplot2 labeller that inserts a single line break into facet
strip labels, splitting at the last word boundary at or before \code{width}
characters. This is the facet equivalent of \code{\link[=pm_gg_break]{pm_gg_break()}}, which
breaks axis and other aesthetic labels on a plot.
}
\examples{
library(ggplot2)

data <- data.frame(
  x = rnorm(60),
  y = rnorm(60),
  grp = rep(c("Short label", "A much longer label for this group"), 30)
)

ggplot(data, aes(x, y)) +
  geom_point() +
  facet_wrap(~grp, labeller = pm_label_break(15))

}
\seealso{
\code{\link[=pm_gg_break]{pm_gg_break()}}, \code{\link[=pm_gg_break_aes]{pm_gg_break_aes()}} to break axis and aesthetic
labels on a plot.
}
