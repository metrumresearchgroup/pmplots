% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pmp-gg-labs.R
\name{pmp_relabel}
\alias{pmp_relabel}
\alias{pmp_relabel.gg}
\alias{pmp_relabel.list}
\title{Relabel a pmplots plot using a yspec object or named list}
\usage{
pmp_relabel(obj, ...)

\method{pmp_relabel}{gg}(obj, spec = list(), labs = list(), ...)

\method{pmp_relabel}{list}(obj, spec = list(), labs = list(), ...)
}
\arguments{
\item{obj}{a gg object created through a \code{pmplots} function or a
list of such objects.}

\item{...}{additional arguments passed to \code{\link[=pmp_gg_labs]{pmp_gg_labs()}}.}

\item{spec}{a named list of label data; names correspond to columns
in the data used to make the plot; may also be a \code{yspec} object, which
will be converted to a named list through \code{\link[yspec:ys_get_short_unit]{yspec::ys_get_short_unit()}}.}

\item{labs}{another named list of label data to override names found in
\code{spec}.}
}
\description{
This function relabels \code{x} and \code{y} axis titles based on the data columns
used to create the plot, looking up in a named list or \code{yspec} object. This
is only for pmplot outputs; consider \code{\link[yspec:ys_gg_labs]{yspec::ys_gg_labs()}} for labeling
aesthetics in an arbitrary \code{gg} object.
}
\examples{
data <- pmplots_data_obs()

p <- dv_pred(data)
p

spec <- list(DV = "CX1123 concentration (ng/mL)")

p <- pmp_relabel(p, spec)
p

}
\seealso{
\code{\link[=pmp_gg_labs]{pmp_gg_labs()}}, \code{\link[=pmp_relabel_wrap]{pmp_relabel_wrap()}}, \code{\link[=pmp_relabel_pairs]{pmp_relabel_pairs()}}
}
